import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("📈 MarketAlphaElite")
                .font(.largeTitle)
                .padding()
            Text("Live Trades • Probable Entries • Alerts")
                .font(.subheadline)
                .foregroundColor(.gray)
        }
    }
}
